---
name: threadcast-logout
description: Log out of ThreadCast from Codex by clearing local ThreadCast credentials.
---

# ThreadCast Logout

Use the `threadcast.logout` MCP tool. Do not use shell commands.

After the tool succeeds, briefly confirm that ThreadCast credentials were cleared.
