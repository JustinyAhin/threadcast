---
name: threadcast-debug
description: Print a ThreadCast diagnostic dump for support and bug reports.
---

# ThreadCast Debug

Use the `threadcast.debug` MCP tool. Do not use shell commands.

Print the returned text dump verbatim inside a fenced code block so the user can copy and paste it.

Tell the user the dump is safe to share publicly — it contains presence-only env flags, file names, durations, and recent log lines, but no auth tokens or session contents.
