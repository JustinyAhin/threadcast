Show the current ThreadCast authentication state and the latest local Claude Code session available to share.

Call `threadcast.status` and summarize the result briefly, including whether login is pending and that rerunning `/threadcast:login` continues it.
